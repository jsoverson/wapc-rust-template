schema: schema.widl
generates:
  src/generated.rs:
    overwrite: true
    module: "@wapc/codegen-rust-guest"
    visitorClass: ModuleVisitor
  src/lib.rs:
    module: "@wapc/codegen-rust-guest"
    visitorClass: ScaffoldVisitor
